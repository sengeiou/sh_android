<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:orientation="vertical" >

    <TextView android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="@string/suggested_people_section_title"
      android:textAppearance="@style/SectionTitleAppearance"
      android:paddingTop="16dp"
      android:paddingLeft="16dp"/>

    <LinearLayout android:id="@+id/suggested_people_list"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="vertical">

        <!-- Los dejo sólo por demostrar cuál es el propósito de este LinearLayout. -->
        <!-- Si se descomenta se puede ver cómo quedaría en la preview. -->
        <!-- Sería deseable borrar tod0 esto y dejar el layuot vacío antes de dar la tarea por finalizada -->

        <!--
        <include layout="@layout/item_list_suggested_user" />

        <include layout="@layout/item_list_suggested_user" />

        <include layout="@layout/item_list_suggested_user" />
        -->
    </LinearLayout>
</LinearLayout>