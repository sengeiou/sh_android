<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="wrap_content">

    <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:orientation="vertical">

        <include
          android:id="@+id/profile_header"
          layout="@layout/fragment_profile_header" />

        <View
          android:layout_width="match_parent"
          android:layout_height="4dp"
          android:layout_below="@+id/profile_header"
          android:background="@drawable/shadow_top_down" />

        <FrameLayout
          android:id="@+id/profile_shots_container"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:layout_below="@+id/profile_header">

            <LinearLayout
              android:id="@+id/profile_shots_list"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:orientation="vertical"
              android:visibility="gone"
              android:showDividers="middle"
              android:divider="?android:listDivider"
              tools:visibility="visible"
              >

                <include layout="@layout/item_list_shot" />

                <include layout="@layout/item_list_shot" />

                <include layout="@layout/item_list_shot" />
            </LinearLayout>

            <TextView
              android:id="@+id/profile_shots_empty"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_gravity="center"
              android:padding="16dp"
              android:text="@string/shot_timeline_empty_title"
              android:textColor="@color/gray_50"
              android:layout_marginTop="16dp"
              android:layout_marginBottom="16dp"
              android:visibility="gone"
              android:textSize="20sp" />
        </FrameLayout>

    </RelativeLayout>
</ScrollView>
