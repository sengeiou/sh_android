package com.shootr.android.domain.exception;

public class DeleteStreamNotAllowedException extends ShootrException {
    //TODO should extend shootr explicit exception so that domain is forced to handle it. But error handling between domain and presentation needs to improve before
}
