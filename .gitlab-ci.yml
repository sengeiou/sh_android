before_script:

features:
    script: ./gradlew test
    only:
        - /^feature\/.*$/

integration:
    script: ./gradlew build -PdisablePreDex
    only:
        - develop
        - /^bugfix\/.*$/

sonar:
    script: ./gradlew sonarRunner
    stage: deploy
    only:
        - develop
        - feature/sonar #TODO for testing purposes, delete before merging
    allow_failure: true