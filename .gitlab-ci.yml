before_script:

features:
    script: ./gradlew test
    only:
        - /^feature\/.*$/

integration:
    script: ./gradlew build -PdisablePreDex
    only:
        - develop
        - /^bugfix\/.*$/
